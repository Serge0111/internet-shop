import { SearchLivePipe } from './search-live.pipe';

describe('SearchLivePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchLivePipe();
    expect(pipe).toBeTruthy();
  });
});
